# SVM 港股交易系统依赖
# 安装: pip install -r requirements.txt

# ── 核心数据与计算 ────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3          # SVM / GridSearchCV / TimeSeriesSplit
joblib>=1.3                # 模型持久化
xgboost>=2.0               # 可选：集成模型

# ── 新闻数据 ──────────────────────────────────────────────────
akshare>=1.12              # 财联社 / 东方财富新闻（免费数据源）

# ── 数据源与 AI 分析 ──────────────────────────────────────────
requests>=2.31
anthropic>=0.30            # 可选：用于扩展AI分析

# ── 可视化 ────────────────────────────────────────────────────
matplotlib>=3.7
plotly>=5.15               # 可选：交互式图表

# ── 可选：TA-Lib（需要系统级安装，如不安装则使用纯 pandas 实现）─
# ta-lib

# ── 开发 / 测试 ───────────────────────────────────────────────
# pytest>=7.0
