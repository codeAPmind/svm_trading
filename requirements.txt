# SVM 港股交易系统依赖
# 安装: pip install -r requirements.txt

# ── 核心数据与计算 ────────────────────────────────────────────
futu-api>=9.1              # Futu OpenD 数据接口（港股行情）
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3          # SVM / GridSearchCV / TimeSeriesSplit
joblib>=1.3                # 模型持久化
tushare>=1.2.89            # A股数据

# ── 新闻数据 ──────────────────────────────────────────────────
akshare>=1.12              # 财联社 / 东方财富新闻（免费数据源）

# ── AI 分析 (DeepSeek API 兼容 OpenAI 格式，直接用 requests) ──
requests>=2.31

# ── 可视化 ────────────────────────────────────────────────────
matplotlib>=3.7
plotly>=5.15               # 可选：交互式图表

# ── 可选：TA-Lib（需要系统级安装，如不安装则使用纯 pandas 实现）─
# ta-lib

# ── 开发 / 测试 ───────────────────────────────────────────────
# pytest>=7.0
